# Refine Design First-Version Review

Owner authorization: after clarifying the original idea was for later, the owner requested a first implementation. This authorizes the planning skill and its narrowly reconciled assessment entry point, not an Alexa run or implementation workflow.

Implemented one SKILL.md and normal-discovery UI metadata. Existing specialist guidance remains canonical in its own resources. Only model/controller entry-point instructions and contracts changed; assessment-only prohibitions remain. No new specialist was installed. The UI designer, view and other unready roles are reported as capability gaps.

Authoring-parent scenario walkthrough:

- Incomplete brief: choose a bounded useful question; provide conditional advice rather than demand a full specification.
- Repeat invocation/no material change: reuse durable findings, including zero new consultations.
- Changed accepted decision: reassess affected advice and preserve unrelated decisions.
- Conflicting recommendations: use responsibility boundaries and owner decisions, not voting or silent acceptance.
- Missing role/tool: report it; no fake named-role output, automatic installation or CLI bypass.
- Owner decision needed: stop dependent refinement while preserving useful independent findings.
- Discussion-only request: return text; do not silently create planning files.
- Explicit documentation request: parent updates product records, keeping proposals separate and preserving evidence beyond temporary files.
- Escalation into coding: no implementation or integrated workflow activation; existing opt-in/readiness gates remain.

Default consultation bound is one initial round and at most one targeted follow-up; this is a provisional efficiency choice, not measured optimal behavior. Normal implicit skill discovery is enabled. Actual role availability can depend on client/session refresh. No claim of independent reviewer certification, live behavior, model cost or readiness for routine multi-agent deployment is made.

Validation passed: bundled quick_validate.py reports a valid skill; 107 local links/anchors across connected resources resolve; updated model/controller TOML definitions parse and retain read-only settings. These are static checks, not live behavioral validation. No live specialist consultation or product changes were performed during creation.

## Product/Architecture Scope Update

Added explicit product and architecture planning scopes with different outputs and default routing. Static scenario review: an early feature brief stays product-focused; a runtime/data-contract question uses architecture planning; a domain meaning question remains a product choice even with model input; a feasibility conflict returns to the owner without silently changing UX; a mixed request labels dependencies without launching both full passes. Invocation examples are natural language, not claimed parser support. No live scope-routing evaluation is claimed.

UI integration update: ui-designer is now installed with explicit standalone/refine-design support and is included in the skill routing table. The earlier missing-role scenario still applies to other unavailable specialties. The bundled skill validator passed after this routing change; no live UI consultation was run.
