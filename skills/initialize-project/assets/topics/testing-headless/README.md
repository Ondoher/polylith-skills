# Testing

## Installed Lane

This headless project uses Jasmine directly. The default test command discovers `src/**/*.spec.js`.

## Commands

```text
npm test
{{COVERAGE_COMMAND}}
```

{{COVERAGE_BODY}}

Behavior, determinism, cleanup, fixtures, failures, and coverage rules come from the canonical [`testing.md`]({{STANDARDS_ROOT_LINK}}/testing.md) standard and the [folder standards manifest](../standards/manifest.md).
