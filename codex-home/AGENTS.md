Review-agent lifecycle is owned by the global `review-standards` skill. When repository bootstrap is requested, the `bootstrap` skill must first load the Codex-root and repository-root instructions. If the repository-root `AGENTS.md` contains the standalone directive `Bootstrap profile: instructions-only`, bootstrap must skip `review-standards`, checkpoint-adviser startup, and generated standards-guide checks and proceed under the loaded instructions. Otherwise, the `bootstrap` skill must execute `review-standards` bootstrap mode. Dedicated standards reviewers and the checkpoint adviser may run only through that skill's eligibility gates.

During a bootstrapped reviewer session, treat a changed `agents/topics/active-topic.md` target or an explicit change of governing topic as a lifecycle event. Before continuing implementation, execute `review-standards` topic-refresh mode so stale reviewer context is invalidated and applicable reviewers are refreshed. This is event-driven orchestration, not a filesystem watcher.
